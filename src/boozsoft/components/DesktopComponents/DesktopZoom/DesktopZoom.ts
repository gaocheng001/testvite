export function abc(zoom,showZoom){
  // const {remote} = require('electron');
  //
  // let webContent = remote.getCurrentWebContents();
  // let timeOut;
  // setInterval(() => {
  //   const _zoom = webContent.getZoomLevel().toFixed(2);
  //   if (zoom.value != _zoom* 100 + 100) {
  //     zoom.value = _zoom * 100 + 100;
  //     showZoom.value = true;
  //     window.clearTimeout(timeOut);
  //     timeOut = setTimeout(() => {
  //       showZoom.value = false;
  //     }, 1000);
  //   }
  // }, 100);
}
